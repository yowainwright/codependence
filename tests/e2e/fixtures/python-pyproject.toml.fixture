[tool.poetry]
name = "test-project"
version = "0.1.0"
description = "Test project for codependence"

[tool.poetry.dependencies]
python = "^3.8"
requests = "^2.28.0"
flask = "^2.0.0"
django = "^3.2.0"

[tool.poetry.dev-dependencies]
pytest = "^7.0.0"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
